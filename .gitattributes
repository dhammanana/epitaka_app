# Shell scripts must keep LF endings even when checked out on Windows, where
# core.autocrlf is on by default on the CI runners. A CRLF shebang and CRLF
# `case` patterns both break silently under Git Bash.
*.sh text eol=lf
